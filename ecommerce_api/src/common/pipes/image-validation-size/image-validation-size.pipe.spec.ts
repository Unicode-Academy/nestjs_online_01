import { ImageValidationSizePipe } from './image-validation-size.pipe';

describe('ImageValidationSizePipe', () => {
  it('should be defined', () => {
    expect(new ImageValidationSizePipe()).toBeDefined();
  });
});
